import { View, Text } from 'react-native';
import React from 'react';
import { ForgotPasswordProps } from '@app-screens/types';

export const ForgotPassword = (props: ForgotPasswordProps) => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};
